# Deployment config. Holds every secret, so this must never be committed.
elastibot.yml
elastibot.yaml
!elastibot.example.yml

# Elastibot no longer reads .env, but kept here so an old one from a pre-YAML checkout can't be committed by accident
.env
.env.*

# Local persistence
data/
*.state.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Tests
coverage/
.jest-cache/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Logs
*.log

# Python venv
.venv/

# Sigma
data/sigma-repo/
data/sigvenv/
data/sigma.db*